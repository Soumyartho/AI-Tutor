fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sympy==1.13.3
python-jose[cryptography]==3.3.0
httpx==0.28.1
slowapi==0.1.9
firebase-admin==6.6.0
instructor==1.7.2
groq==0.15.0

# dev/test
pytest==8.3.4

# NOTE: Phase 2 (US-009 clustering) semantic-embedding deps live in
# requirements-ml.txt, not here. sentence-transformers pulls in PyTorch
# (~1-2GB) which blows past free-tier build limits on Render/Railway for
# little benefit — the app falls back to a deterministic hashing embedder +
# pure-python KMeans automatically when those packages aren't installed.
# Only install requirements-ml.txt if you have a host that can handle it.
